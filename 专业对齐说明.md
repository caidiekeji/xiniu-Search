# xiniubot 专业搜索引擎功能对齐（第五轮交付说明）

本批次在既有管理后台之上，按"专业搜索引擎"能力补齐了抓取、索引、排序、检索、工程化五个方面的差距。
涉及改动均已完成并通过回归测试（`tests/test_pro_alignment.py`，32 项断言全绿），真实爬取链路已验证。

---

## 一、本批次新增/增强的能力

| 能力 | 落地位置 | 说明 |
| --- | --- | --- |
| 断点续爬 | `main.py` / `crawler/frontier.py` | 启动时若存在 `data/frontier.json` 则恢复 seen 集合、待爬队列与累计计数，再并入新种子（按指纹去重）。机制此前已存在但从未被调用，现已接通。后台"爬虫控制 → 断点续爬"开关（默认开）。 |
| URL 规范化/追踪参数清洗 | `crawler/frontier.py::_clean_query` | 剔除 utm_*、fbclid、gclid、dclid、msclkid、mc_cid/eid、ref、spm、source、from、share_token、sns 等 17 个追踪参数，保留其余参数原序（`config.URL_TRACKING_PARAMS` 可配）。 |
| 内容近似去重 | `indexer/simhash.py` + `search/engine.py::check_duplicate` | 64 位 SimHash，抓取时与已索引文档做海明距离比对，<= 阈值（默认 3）判为重复并跳过。`config.DEDUP` 可配开关与阈值。 |
| 主内容提取 | `crawler/parser.py::_extract_main_text` | 优先 article/main 容器（>=100 字）；否则按 p/h1-h6/blockquote/pre/li 块做文本密度过滤（跳过 <20 字、链接占比 >60%、nav/footer/header/aside 父级）；兜底整页文本。 |
| 字段加权排序 | `indexer/ranker.py` 重写 | 标题/描述命中按 `RANKING.title_weight(2.5)/description_weight(1.5)` 放大，正文基准 1.0；标题/描述词单独进倒排，保证"仅字段命中"可检索。 |
| 链接权威分 | `indexer/authority.py` + 后台"重建权威分" | 在已索引文档的出链-入链关系上迭代简化 PageRank（阻尼 0.85，15 轮），min-max 归一化到 [0,1] 写回 `Document.authority`；爬虫任务结束自动重建，也可后台手动重建。排序按 `RANKING.authority_weight(0.35)` 融合。 |
| 时间衰减 | `indexer/ranker.py::_finalize` | `RANKING.time_decay_days > 0` 时按抓取时间衰减（下限 0.5），默认关闭。 |
| 高级查询语法 | `search/engine.py::parse_query` | `"精确短语"`、`-排除`、`A OR B`、`site:域名`、`intitle:词`、`inurl:串`，可组合。 |
| 拼写纠错 + 搜索建议 | `search/engine.py::correct/suggest` + `search_server.py` | `/api/correct`（did-you-mean，无结果时提示）、`/api/suggest`（前端输入自动补全下拉，按词频排序）。 |
| 搜索缓存 | `search_server.py::_LRU` | 查询/API 结果 LRU 缓存（256 条），索引文件 mtime 变化自动失效。 |
| 后台配置扩展 | `admin_config.py` / `admin.js` | 新增"排序加权""内容去重"两组配置表单；文档列表新增权威列，文档详情展示权威分/SimHash/出链数/抓取时间。 |

## 二、新增可配置项（后台"爬虫控制 → 运行参数配置"或 `data/admin/config.json`）

```jsonc
{
  "RANKING": {
    "title_weight": 2.5,          // 标题命中权重
    "description_weight": 1.5,    // 描述命中权重
    "body_weight": 1.0,           // 正文权重
    "authority_weight": 0.35,     // PageRank 融合强度, 0 关闭
    "time_decay_days": 0          // 时间衰减天数, 0 关闭
  },
  "DEDUP": {
    "enabled": true,              // 内容近似去重开关
    "simhash_threshold": 3        // 海明距离阈值, <= 判为重复
  }
}
```

## 三、使用方式

```bash
# 搜索服务 (5050) — 已支持高级语法、建议下拉、纠错提示、结果缓存
.venv\Scripts\python.exe search_server.py

# 管理后台 (127.0.0.1:8081) — 密码见 data\admin\admin_password.txt
.venv\Scripts\python.exe admin_server.py

# 命令行爬虫 (断点续爬默认开启; resume=False 可强制重爬)
.venv\Scripts\python.exe main.py --seeds "https://example.com/" --max-pages 100 --max-depth 3

# 回归测试
.venv\Scripts\python.exe tests\test_pro_alignment.py
```

## 四、验证结果

- `tests/test_pro_alignment.py`：32 项断言全部通过（URL 清洗 / 续爬 roundtrip / resume 语义 / SimHash 去重 / 字段加权排序 / 权威分归一化与相对权威 / 查询语法解析 / 后台 RANKING-DEDUP 配置与重建权威分 API / resume 透传 / 搜索建议·纠错·缓存 API）。
- 真实链路：首次抓取 example.com 入库（含 simhash/出链/字段词），二次相同抓取日志输出 `[DEDUP] 内容近似重复, 跳过`，文档数不变。
- 全部改动文件 `py_compile` 通过，`static/admin.js` 通过 `node --check`。

## 五、已知边界 / 待补齐（相对真正商业搜索引擎）

- **分词质量**：未登录中文词按 HMM 拆分（如"旅游"→旅/游），影响词组粒度；可通过扩充 `data/dict/dict.txt` 词典缓解。
- **索引为内存全量 + pickle 快照**：无段合并/压缩、无 mmap 增量索引，超过数十万文档需换磁盘索引（建议后端换 Lucene/ES）。
- **PageRank 仅用已索引文档间的链接**：未考虑外链权值归一与种子集外链接，是受限近似。
- **纠错仅支持词典编辑距离**（中文不做纠错）；建议、纠错基于词频而非点击/日志。
- **无分布式、无去重队列持久化以外的容错**（单进程单线程任务模型）；robots 遵守与限速已有。
- **搜索结果无锚文本上下文、无同义词/相关搜索、无图片/富结果**。
- 旧索引中历史文档无新字段（title_tokens/authority/simhash 等），排序层已做防御性 `getattr` 兼容；对新爬取文档完整生效。

## 六、本轮改动文件清单

- 改：`config.py`、`crawler/frontier.py`、`crawler/parser.py`、`indexer/inverted_index.py`、`indexer/ranker.py`、`search/engine.py`、`main.py`、`search_server.py`、`admin_server.py`、`admin_runner.py`、`admin_config.py`、`static/admin.js`
- 新：`indexer/simhash.py`、`indexer/authority.py`、`tests/test_pro_alignment.py`
